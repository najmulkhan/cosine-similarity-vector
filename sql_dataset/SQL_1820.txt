Topic: WHERE Clause
Difficulty: Medium

Explanation:
The WHERE clause filters rows based on a condition.

Question:
Write an SQL query related to where clause using the 'grades' table.

Step-by-Step Solution:
1. Identify the relevant table and columns.
2. Apply the concept of where clause appropriately.
3. Ensure the query follows ANSI SQL syntax.
4. Validate results with realistic data.

Answer:
-- Example ANSI SQL query
SELECT * FROM grades;

Example:
This would return all rows from the 'grades' table.

Tags: WHERE CLAUSE, SQL, GRADES
