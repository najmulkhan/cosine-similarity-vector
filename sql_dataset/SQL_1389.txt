Topic: Subqueries
Difficulty: Easy

Explanation:
A subquery is a query nested within another SQL query.

Question:
Write an SQL query related to subqueries using the 'transactions' table.

Step-by-Step Solution:
1. Identify the relevant table and columns.
2. Apply the concept of subqueries appropriately.
3. Ensure the query follows ANSI SQL syntax.
4. Validate results with realistic data.

Answer:
-- Example ANSI SQL query
SELECT * FROM transactions;

Example:
This would return all rows from the 'transactions' table.

Tags: SUBQUERIES, SQL, TRANSACTIONS
