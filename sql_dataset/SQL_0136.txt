Topic: ALTER TABLE
Difficulty: Easy

Explanation:
ALTER TABLE modifies an existing table structure.

Question:
Write an SQL query related to alter table using the 'courses' table.

Step-by-Step Solution:
1. Identify the relevant table and columns.
2. Apply the concept of alter table appropriately.
3. Ensure the query follows ANSI SQL syntax.
4. Validate results with realistic data.

Answer:
-- Example ANSI SQL query
SELECT * FROM courses;

Example:
This would return all rows from the 'courses' table.

Tags: ALTER TABLE, SQL, COURSES
