Topic: ORDER BY
Difficulty: Medium

Explanation:
ORDER BY sorts the result set in ascending or descending order.

Question:
Write an SQL query related to order by using the 'customers' table.

Step-by-Step Solution:
1. Identify the relevant table and columns.
2. Apply the concept of order by appropriately.
3. Ensure the query follows ANSI SQL syntax.
4. Validate results with realistic data.

Answer:
-- Example ANSI SQL query
SELECT * FROM customers;

Example:
This would return all rows from the 'customers' table.

Tags: ORDER BY, SQL, CUSTOMERS
