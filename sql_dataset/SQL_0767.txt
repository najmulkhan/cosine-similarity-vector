Topic: DROP TABLE
Difficulty: Easy

Explanation:
DROP TABLE removes an existing table and its data.

Question:
Write an SQL query related to drop table using the 'posts' table.

Step-by-Step Solution:
1. Identify the relevant table and columns.
2. Apply the concept of drop table appropriately.
3. Ensure the query follows ANSI SQL syntax.
4. Validate results with realistic data.

Answer:
-- Example ANSI SQL query
SELECT * FROM posts;

Example:
This would return all rows from the 'posts' table.

Tags: DROP TABLE, SQL, POSTS
